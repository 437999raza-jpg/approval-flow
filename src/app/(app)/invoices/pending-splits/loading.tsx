import { AppPageLoading } from "@/components/AppPageLoading";

export default function Loading() {
  return (
    <AppPageLoading
      title="Opening split review"
      description="Loading uploads that need to be split into separate invoices…"
    />
  );
}
