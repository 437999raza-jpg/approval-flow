import { AppPageLoading } from "@/components/AppPageLoading";

export default function Loading() {
  return (
    <AppPageLoading
      title="Opening reports"
      description="Loading saved reports, filters, projects, and team members…"
    />
  );
}
