import { AppPageLoading } from "@/components/AppPageLoading";

export default function Loading() {
  return (
    <AppPageLoading
      title="Opening statements"
      description="Loading statement reconciliation tools and recent uploads…"
    />
  );
}
