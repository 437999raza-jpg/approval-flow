import { AppPageLoading } from "@/components/AppPageLoading";

export default function Loading() {
  return (
    <AppPageLoading
      title="Opening queue"
      description="Loading uploads, emails, and processing status…"
    />
  );
}
